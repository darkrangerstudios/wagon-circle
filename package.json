{
  "name": "wagon-circle",
  "displayName": "Wagon Circle",
  "description": "One VS Code room for you, Claude and Codex. Both agents run as private child processes over stdio: no daemon, no open ports.",
  "version": "0.5.0",
  "publisher": "darkrangerstudios",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "vscode": "^1.90.0"
  },
  "main": "./src/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "wagonCircle.newRoom",
        "title": "Wagon Circle: New Room"
      },
      {
        "command": "wagonCircle.joinExisting",
        "title": "Wagon Circle: Join Existing Conversations (fork)"
      },
      {
        "command": "wagonCircle.openRoom",
        "title": "Wagon Circle: Reopen a Room"
      },
      {
        "command": "wagonCircle.checkSetup",
        "title": "Wagon Circle: Check Setup"
      }
    ],
    "configuration": {
      "title": "Wagon Circle",
      "properties": {
        "wagonCircle.userName": {
          "type": "string",
          "default": "",
          "description": "Your name as the agents see it and as the room labels your messages. Empty = first name from git config user.name, else your login name."
        },
        "wagonCircle.claudeModel": {
          "type": "string",
          "default": "claude-sonnet-5",
          "description": "Model alias or id passed to claude --model."
        },
        "wagonCircle.claudePath": {
          "type": "string",
          "default": "",
          "description": "Path to the claude CLI. Empty = the newest one found (the VS Code Claude extension's bundled CLI or ~/.local/bin/claude)."
        },
        "wagonCircle.codexPath": {
          "type": "string",
          "default": "",
          "description": "Path to the codex CLI. Empty = ~/.local/bin/codex, then the standalone install, then PATH."
        },
        "wagonCircle.hopCap": {
          "type": "number",
          "default": 2,
          "minimum": 0,
          "maximum": 20,
          "description": "Max agent-to-agent hand-offs per message you send."
        },
        "wagonCircle.cwd": {
          "type": "string",
          "default": "",
          "description": "Working folder both agents may read. Empty = first workspace folder."
        },
        "wagonCircle.defaultTarget": {
          "type": "string",
          "enum": [
            "claude",
            "codex",
            "both"
          ],
          "default": "claude",
          "description": "Who leads new rooms: messages with no @mention go to the lead. Change per room with the Lead chip or /default."
        },
        "wagonCircle.bothMode": {
          "type": "string",
          "enum": [
            "sequential",
            "parallel"
          ],
          "default": "sequential",
          "description": "How @both works: sequential = take turns, the second sees the first answer; parallel = answer at once. Change per room with /both."
        },
        "wagonCircle.ideContext": {
          "type": "boolean",
          "default": true,
          "description": "Attach what you are looking at in VS Code (active file, selection or visible lines, open tabs, problems) to each message. Toggle per room with the \ud83d\udccd chip."
        },
        "wagonCircle.claudeEffort": {
          "type": "string",
          "enum": [
            "",
            "low",
            "medium",
            "high",
            "max"
          ],
          "default": "",
          "description": "Claude effort for new rooms. Empty = Claude Code default. Set from a room with \"Use for new rooms\"."
        },
        "wagonCircle.codexModel": {
          "type": "string",
          "default": "",
          "description": "Codex model for new rooms (e.g. gpt-6-astra). Empty = your Codex config default. Set from a room with \"Use for new rooms\"."
        },
        "wagonCircle.taskMode": {
          "type": "string",
          "enum": [
            "auto",
            "chat",
            "work"
          ],
          "default": "auto",
          "description": "Auto: a task starts when an agent asks the other for help. Chat: one consultation per message, no task. Work: every message starts a task."
        },
        "wagonCircle.taskDefaults": {
          "type": "object",
          "default": {
            "turns": 20,
            "reserve": 2,
            "minutes": 30
          },
          "description": "Allowances for new tasks: agent turn starts (turns), how many of them are kept for wrapping up (reserve), and active minutes. Set from a room with \"Save as my defaults\"."
        },
        "wagonCircle.codexEffort": {
          "type": "string",
          "default": "",
          "description": "Codex reasoning effort for new rooms. Empty = the model default."
        }
      }
    },
    "walkthroughs": [
      {
        "id": "wagonCircle.start",
        "title": "Get started with Wagon Circle",
        "description": "Claude and Codex in one room, working on local sessions.",
        "steps": [
          {
            "id": "setup",
            "title": "Get your agents ready",
            "description": "Check that the Claude Code and Codex command-line tools are installed and signed in on this host.\n[Check setup](command:wagonCircle.checkSetup)",
            "media": {
              "markdown": "walkthrough/setup.md"
            },
            "completionEvents": [
              "onCommand:wagonCircle.checkSetup"
            ]
          },
          {
            "id": "sessions",
            "title": "Choose where the work happens",
            "description": "Each agent has its own local working session: new, continued or forked.",
            "media": {
              "markdown": "walkthrough/sessions.md"
            }
          },
          {
            "id": "tasks",
            "title": "Stay in the conversation while work continues",
            "description": "Tasks, allowances, Pause and Stop. Local sessions only.",
            "media": {
              "markdown": "walkthrough/tasks.md"
            }
          },
          {
            "id": "try",
            "title": "Try one small handoff",
            "description": "A small read-only review in a new room.\n[New room](command:wagonCircle.newRoom)",
            "media": {
              "markdown": "walkthrough/try-it.md"
            },
            "completionEvents": [
              "onCommand:wagonCircle.newRoom"
            ]
          }
        ]
      }
    ]
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/darkrangerstudios/wagon-circle"
  },
  "homepage": "https://darkrangerstudios.github.io/wagon-circle/",
  "author": "Dean Buttry"
}
